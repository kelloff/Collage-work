# Вставить внутрь server { listen 443 ssl; ... } в блок location / { ... }
# сразу после proxy_set_header X-Forwarded-Proto

    proxy_connect_timeout 600s;
    proxy_send_timeout    600s;
    proxy_read_timeout    600s;
    send_timeout          600s;
    proxy_buffering       off;
